import { NavigationFocusDirective } from './navigation-focus.directive';

describe('NavigationFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationFocusDirective();
    expect(directive).toBeTruthy();
  });
});
