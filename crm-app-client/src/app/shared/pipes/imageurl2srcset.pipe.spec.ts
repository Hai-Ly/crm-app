import { Imageurl2srcsetPipe } from './imageurl2srcset.pipe';

describe('Imageurl2srcsetPipe', () => {
  it('create an instance', () => {
    const pipe = new Imageurl2srcsetPipe();
    expect(pipe).toBeTruthy();
  });
});
